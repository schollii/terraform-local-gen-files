apiVersion: v1
kind: Service
metadata:
  name: {{ include "your-micro-svc1.fullname" . }}
  labels:
    {{- include "your-micro-svc1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "your-micro-svc1.selectorLabels" . | nindent 4 }}
