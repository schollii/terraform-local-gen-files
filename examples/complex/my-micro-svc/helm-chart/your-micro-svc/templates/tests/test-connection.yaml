apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "your-micro-svc1.fullname" . }}-test-connection"
  labels:
    {{- include "your-micro-svc1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "your-micro-svc1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
